import 'package:flutter/material.dart';

class StopwatchBody extends StatelessWidget {
    const StopwatchBody({Key? key}) : super(key: key);
    @override
    Widget build(BuildContext context) {
        return Center(
          child: Text('Stopwatch body'),
        );
    }
}